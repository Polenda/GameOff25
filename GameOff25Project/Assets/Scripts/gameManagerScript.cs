using UnityEngine;

/*

gameManagerScript handles the overall game management tasks.
|- playerManagerScript manages calling of the player scripts
|-- playerAbilitiiesScript manages the 3 moves of the player
|-- playerPowerupsScript manages the function of each powerup
|-- playerScoreScript will count if the player gains score
|- busDriverManagerScript manages the bus drivers anger level
|- pedestrianManagerScript manages the pedestrians movement and animations
busStopManagerScript manages the level where you buy powerups
sceneChangerScript manages changing between scenes

*/

public class gameManagerScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
